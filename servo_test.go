package gobotGPIO

import (
	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

var _ = Describe("Gobot-Servo Driver", func() {
	PIt("Exposes a struct to create a new Servo driver", func() {
		Expect(true)
	})
})
